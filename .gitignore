# Python
__pycache__/
*.py[cod]
*.sqlite3
.env
.venv/
venv/
ENV/
build/
dist/
*.egg-info/

# Test / lint tool caches and coverage output
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Claude Code
.claude/

# Editor
.vscode/

# macOS
.DS_Store

# One-off scratch: benchmarked Bedrock models to pick the one wired into rag.py.
# Kept locally, not part of the deployed app.
backend/compare.py

# Build-time only: embed_chunks.py writes it, export_index.py turns it into
# backend/data/index/ (which IS tracked, and is what ships in the image).
# Regenerate with `python embed_chunks.py`.
backend/chroma_db/

# AWS CLI scratch output
/response.json

# Root-level pipeline outputs (regenerate with rec_sys.py)
# The runtime copies backend/models_all.joblib, backend/interactions.csv and
# backend/data/players.csv are tracked intentionally (they ship in the container).
/models_all.joblib
/interactions.csv
/players.csv
/master_stats.csv
/master_clustered.csv
/master_team_stats.csv
/master_team_clustered.csv
/adv_stats_combined.csv
/per_100_combined.csv
/per_36_combined.csv
